{% extends "SmartUnityUtilisateurBundle::layout_profil.html.twig" %}
{#{% form_theme form 'SmartUnityUtilisateurBundle:Form:form_div_layout.html.twig' %}#}

{% block edition %}

{#<form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit">
    {{ form_widget(form) }}
    <div>
        <input class="btn" type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
</form> #}

<form action="{{ path('smart_unity_utilisateur_setinfos') }}" {{ form_enctype(form_infos) }} method="POST">

{#{{form_start(form_infos)}}#}
    {{form_errors(form_infos)}}

    <div class="row  ">
        <div class="grille-1">
        {{form_label(form_infos.username)}}
        </div>
        <div class="grille-2">
        {{form_widget(form_infos.username)}}
        </div>
    </div>

    <div class="row  ">
        <div class="grille-1">
        {{form_label(form_infos.nom)}}
        </div>
        <div class="grille-2">
        {{form_widget(form_infos.nom)}}
        </div>
    </div>

    <div class="row  ">
        <div class="grille-1">
        {{form_label(form_infos.email)}}
        </div>
        <div class="grille-2">
        {{form_widget(form_infos.email)}}
        </div>
    </div>

    <div class="row  ">
        <div class="grille-1">
        {{form_label(form_infos.sexe)}}
        </div>
        <div class="grille-2">
        {{form_widget(form_infos.sexe)}}
        </div>
    </div>    

    <div class="row  ">
        <div class="grille-1">
        {{form_label(form_infos.date_naissance)}}
        </div>
        <div class="grille-2">
        {{form_widget(form_infos.date_naissance)}}
        </div>
    </div>

    <div class="row  ">
        <div class="grille-1">Appareil:</div>
        <div class="grille-2"></div>
    </div>
    <div class="row  ">
        <div class="grille-1">Un peu plus sur moi:</div>
        <div class="grille-2"></div>
    </div>

    <div>
        <input type="submit" value="Modifier mes infos" />
    </div>   
    {{form_end(form_infos)}}  
</form>
    <input class="btn" type="button" value="retour" onclick="javascript:history.back()">
    <a class="active" href="{{ path('fos_user_change_password') }}">Changer mon mot de passe</a>
{% endblock %}   

{% block preferences %}  

 <form action="{{ path('smart_unity_utilisateur_setpref') }}" {{ form_enctype(form_pref) }} method="POST">
    {{form_errors(form_pref)}}
    
    {{form_row(form_pref.pref_mp)}}
    {{form_row(form_pref.pref_comm)}}
    {{form_row(form_pref.pref_smartcafe)}}
    {{form_row(form_pref.pref_rep)}}
    {{form_row(form_pref.pref_repValidee)}}
    {{form_row(form_pref.pref_repCertifiee)}}
    <div>
        <input type="submit" value="Modifier mes preferences" />
    </div> 

{{form_end(form_pref)}}   
</form> 

{% endblock %}